{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch BoxPilot API",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/server",
      "cwd": "${workspaceFolder}/server",
      "env": {
        "ADDR": ":8080",
        "DB_PATH": "${workspaceFolder}/data/app.db"
      }
    }
  ]
}
