# 开发：Vite 把 /api、/healthz 代理到该后端（仅 npm run dev）
DEV_API_ORIGIN=http://192.168.50.145:8080

# 与生产 VITE_API_ORIGIN 对应；开发一般留空，走相对路径 + 上面代理
# VITE_API_ORIGIN=

# 浏览器里 axios 请求的 API 前缀（经 dev server，仍走 /api → DEV_API_ORIGIN）
VITE_API_BASE=/api/v1
